@inherits BaseButtonComponent

<ButtonComponent Caption="@(Caption ?? "New")"
				 Color="@(Color ?? "secondary")"
				 Icon="@(Icon ?? "pencil-square-o")"
				 OnClick="OnClick"
				 ToolTipStr="@(ToolTipStr ?? "New")"
				 IsOutline="@IsOutline"
				 IsSmall="@IsSmall" />